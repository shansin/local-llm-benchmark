# Generated by tasks/generators/build.py — do not edit by hand.
# The answer key below is computed from the same data that builds the
# prompt, so the two cannot drift apart.

category = "transformation"
difficulty = "medium"

prompt = '''
Below are 28 lines of `id,hours,rate`.

Produce a Markdown table with exactly these columns, in this order:

| ID | Hours | Rate | Pay | Band |

- `Pay` is hours multiplied by rate, rounded to two decimal places.
- `Band` is `low` if Pay is under 500, `mid` if Pay is 500 up to but not including 1500,
  and `high` if Pay is 1500 or more.

Output one row per input line, all 28 of them, in the original order. Output the
table and nothing else — no heading, no explanation, no note about the remaining rows.

DATA
E2001,40,18.85
E2002,18,23.38
E2003,33,47.64
E2004,29,18.25
E2005,23,41.83
E2006,45,37.04
E2007,31,47.49
E2008,41,23.4
E2009,14,60.8
E2010,22,34.24
E2011,29,33.7
E2012,18,20.44
E2013,32,31.26
E2014,42,38.45
E2015,15,36.11
E2016,13,25.5
E2017,36,22.03
E2018,39,31.29
E2019,15,59.02
E2020,32,37.36
E2021,20,24.76
E2022,32,46.43
E2023,44,59.68
E2024,32,33.06
E2025,13,27.41
E2026,33,20.69
E2027,26,59.72
E2028,26,36.01
'''

criteria = '''
Evaluation criteria:
- Exactly 28 data rows, in the original order, plus the header and separator rows.
  A table that stops early or ends with an ellipsis row has failed regardless of how correct
  the rows it did write are.
- `Pay` must be hours x rate to two decimals, and `Band` must follow the stated thresholds.
  The boundaries are inclusive-below: 500 is `mid`, 1500 is `high`.
- No prose before or after the table.
- For reference the correct band distribution is 5 low, 18 mid,
  5 high.
'''

[[checks]]
type = "line_count"
pattern = "^\\s*\\|"
equals = 30
weight = 2.0

[[checks]]
type = "match_count"
pattern = "\\bE2\\d{3}\\b"
equals = 28

[[checks]]
type = "match_count"
pattern = "(?i)\\blow\\b"
equals = 5

[[checks]]
type = "match_count"
pattern = "(?i)\\bmid\\b"
equals = 18

[[checks]]
type = "match_count"
pattern = "(?i)\\bhigh\\b"
equals = 5
